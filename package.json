{
  "name": "wix-static-scraper",
  "version": "1.0.0",
  "description": "Crawl a Wix site and save it as a static site",
  "scripts": {
    "crawl": "node scripts/crawl.js"
  },
  "dependencies": {
    "puppeteer": "^21.0.0"
  }
}
